apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: zerodowntime
spec:
  hosts:
  - "*"
  gateways:
  - zerodowntime-gateway
  http:
  - match:
    - uri:
        prefix: /    
    route:
    - destination:
        host: zerodowntime
        port:
          number: 8882
